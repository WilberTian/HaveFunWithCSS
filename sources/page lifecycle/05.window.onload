<!DOCTYPE html>
<html>
  <head>
  </head>
  <body>
    <script>
      window.onload = function () {
        alert('Page loaded');

        // image is not yet loaded (unless was cached), so the size is 0x0
        alert('image size is: ' + img.width + '*' + img.height);
      };
    </script>

    <img id="img" src="https://en.js.cx/clipart/train.gif?speed=1&cache=0">
  </body>
</html>